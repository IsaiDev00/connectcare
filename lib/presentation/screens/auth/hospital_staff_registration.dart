import 'package:flutter/material.dart';

class HospitalStaffRegistration extends StatelessWidget {
  const HospitalStaffRegistration({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Registro Personal Hospitalario'),
      ),
      body: Center(
        child: Text('Pantalla de Registro para el Personal Hospitalario'),
      ),
    );
  }
}
